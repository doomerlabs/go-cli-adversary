{
  "name": "go-cli",
  "version": "0.0.34",
  "description": "Reviews Go command-line applications for predictable configuration, cancellation, diagnostics, and exit behavior.",
  "type": "module",
  "private": true,
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/doomerlabs/go-cli-adversary.git"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit && node scripts/build.mjs",
    "prepare": "npm run build",
    "test": "npm run build && tsx --test test/*.test.ts"
  },
  "dependencies": {
    "@adversarylabs/sdk": "^0.1.19",
    "web-tree-sitter": "^0.26.5"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "esbuild": "^0.28.1",
    "tree-sitter-go": "^0.25.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  }
}
